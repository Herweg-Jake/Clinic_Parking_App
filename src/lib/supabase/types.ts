// Supabase database types
// You can generate these automatically with: npx supabase gen types typescript --local

export type Database = {
  public: {
    Tables: {
      // Add your Supabase tables here if needed
      // For now, we're using Prisma for all database operations
    };
  };
};
